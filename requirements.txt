# Core runtime AND development/CI dependencies — data extractors, backtests,
# the front-test runner, and the quality gates.
#
# DEPS-001: the LIVE RUNTIME block below is pinned to the exact versions
# verified together on the live runner (pip freeze, 2026-07-07). This is
# live-money code — a reinstall must never silently pick up a new
# pandas/dhanhq mid-week. Bump these deliberately in a dependency-maintenance
# PR and re-run the full gate suite (CI installs this file, so the gates always
# test the pinned set).
#
# The DEVELOPMENT block was merged in from requirements-dev.txt (PR #125) to
# cut the number of requirement files. Two consequences to know about:
#   * `pip install -r requirements.txt` on the live box now also installs the
#     test/lint toolchain and its transitive tree. That is accepted, not an
#     oversight — but it means this file is no longer a literal pip-freeze of
#     the trading process, so read DEPS-001 as applying to the first block.
#   * CI's `pip_audit --local` now sees the dev tooling too, so an advisory in
#     e.g. bandit can fail the build. Judge such a finding on whether it
#     touches the runtime block before treating it as a trading risk.

# ---- LIVE RUNTIME (DEPS-001: the verified live-runner set) -------------------
dhanhq==2.2.0
pandas==3.0.5
numpy==2.4.6
backtesting==0.6.6
python-dotenv==1.2.2
gspread==6.2.1
# Timezone data. Bumping this is desirable in its own right: every trading-day
# boundary, the 15:15 square-off and the pre-open note's date gate are computed
# in Asia/Kolkata, so stale tz data is a correctness risk rather than a chore.
pytz==2026.3.post1
# The broker, symbol-master and Telegram HTTP paths all use requests. Keep this
# at the newest release that clears the repository audit (2.33.0 was the first
# past PYSEC-2026-2275; 2.34.2 is the current audited pin).
requests==2.34.2
# The vendored Shoonya client imports websocket at module load. Keep this
# harmless, conflict-free import dependency in the core set so diagnostics and
# the broker coverage suite work even before optional live credentials exist.
websocket-client==1.8.0
# dhanhq.marketfeed (the websocket market data producer) hard-imports the async
# `websockets` library at package import time. It already arrives transitively
# with dhanhq, but per DEPS-001 the exact version the live runner was verified
# with must be pinned here rather than left to transitive resolution.
websockets==17.0.1
TA-Lib==0.6.8

# ---- DEVELOPMENT / CI QUALITY GATES -----------------------------------------
# Versions mirror the Streamlit Scanner App's verified set where shared.
# `ruff` is pinned in TWO places: here and as the `ruff-pre-commit` rev in
# .pre-commit-config.yaml. Bump both together — the hook is only useful if it
# enforces the same ruleset as CI, and
# `test_precommit_ruff_rev_matches_the_requirements_dev_pin` fails the build if
# they separate.
pytest==9.1.1
ruff==0.16.3
mypy==1.20.2
bandit==1.9.4
pre-commit==4.6.2
coverage==7.15.4
pytest-cov==7.1.0
pip-audit==2.10.1
# Type stubs for typed third-party usage in the mypy scope. pandas-stubs is
# pinned so local and CI mypy see the exact same pandas typing (an out-of-band
# stubs install once made local mypy fail while CI stayed green).
# mypy and pandas-stubs majors are deliberately NOT auto-bumped (see
# .github/dependabot.yml); take them as dedicated typing upgrades.
types-requests==2.33.0.20260712
pandas-stubs==2.3.3.260113

# ---- Exact optional dependency sets -----------------------------------------
# Live broker compatibility manifest:
#   requirements-brokers.txt
# Kotak's official v2.0.1 tag declares dependencies that conflict with this
# audited set, so do not install both files together. README.md gives the
# safe per-broker commands. Shoonya's NorenApi client remains vendored under
# "Dependencies/Shoonya API/".
#
# Both optional in-process AI agents (SL Hunting + CPR Codex):
#   pip install -r requirements-ai.txt
# This pins the Claude Agent SDK, the Codex runtime, and the MCP/schema stack
# they share. Each strategy stays disabled when its half of the set is absent.
#
# ML Ensemble signal generator:
#   scikit-learn==1.8.0
